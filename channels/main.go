package main

import (
	"fmt"
	"net/http"
	"time"
)

func main() {
	links := []string{
		"http://google.com",
		"http://facebook.com",
		"http://stackoverflow.com",
		"http://golang.org",
		"http://amazon.com",
	}

	s_channel := make(chan string)

	for _, link := range links {
		go checkLink(link, s_channel)
	}

	for l := range s_channel {
		go func(link string) {
			time.Sleep(5 * time.Second)
			checkLink(link, s_channel)
		}(l)
	}
}

func checkLink(link string, c chan string) {
	_, err := http.Get(link)

	if err != nil {
		fmt.Println(link, "might be down!")
		c <- link
		return
	}

	fmt.Println(link, "is up!")
	c <- link
}
